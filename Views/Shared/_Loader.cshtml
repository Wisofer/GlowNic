@* Loader global - Puede ser implementado más adelante *@
<div id="globalLoader" class="hidden fixed inset-0 z-[9999] flex items-center justify-center bg-black/70 backdrop-blur-sm">
    <div class="relative">
        <div class="w-20 h-20 rounded-xl bg-gradient-to-br from-yellow-700 to-yellow-900 flex items-center justify-center shadow-2xl border-2 border-pink">
            <svg class="w-16 h-16 text-pink animate-spin" viewBox="0 0 100 100" fill="currentColor">
                <path d="M50 10 L30 85 L50 90 L70 85 Z" fill="currentColor" opacity="0.9"/>
            </svg>
        </div>
    </div>
</div>

